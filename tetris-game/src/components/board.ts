import React from 'react';

const Board: React.FC = () => {
    return (
        <div className="board">
            {/* Game board will be rendered here */}
        </div>
    );
};

export default Board;